{
  "name": "elm-decimal",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:erosson/decimal.git",
  "author": "Evan <github@erosson.org>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "[ -d .git ] && git config core.hooksPath ./git-hooks",
    "test": "elm-format --validate src tests && elm-test"
  },
  "dependencies": {
    "elm": "^0.19.0-bugfix6",
    "elm-format": "^0.8.1",
    "elm-test": "^0.19.0-rev4"
  }
}
